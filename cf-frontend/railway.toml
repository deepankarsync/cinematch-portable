[build]
dockerfile = "Dockerfile"

[deploy]
# Railway injects $PORT automatically — uvicorn picks it up from the CMD
startCommand = "uvicorn app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/docs"
healthcheckTimeout = 30   # seconds — allow time for pickle files to load
